# SmartSync Migration Manager Dependencies

# Alembic (database migrations)
alembic==1.13.1

# SQLAlchemy (ORM)
sqlalchemy==2.0.30

# Database drivers
psycopg2-binary==2.9.9
asyncpg==0.29.0

# YAML parsing (for config.yaml)
PyYAML==6.0.1

# Environment variables
python-dotenv==1.0.1
